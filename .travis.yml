sudo: required
services:
    - docker

before_install:
    - docker build -t pkn2009/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true pkn2009/docker-react npm run test -- --coverage
